# UXUS — Agent Services

> Pay-per-call primitives for AI agents. Inference, scraping, extraction, embeddings, search, and wallet risk scoring. Paid with USDC on Base via x402 — no accounts, no API keys, no signup.

## Why
An agent with a USDC wallet can use every service here immediately. No provider accounts to create, no keys to rotate, no subscriptions. Payment is authentication.

## Storefront
GET https://uxus.finance/api/catalog  (FREE)
Returns every service, price, and parameter in one call.

## Services

### LLM inference — $0.01
POST /api/llm
Body: { "prompt": "..." }  or  { "messages": [...] }
Optional: model, max_tokens (max 2000)
Returns: { model, content, usage, latency_ms }

### Scrape — $0.01
GET /api/scrape?url=https://example.com&format=markdown
format: markdown | text | html. Optional max_chars.
Returns: { url, title, description, content, chars }

### Extract — $0.01
POST /api/extract
Body: { "schema": { "title": "string", "price": "number" }, "url": "..." }  (or "text")
Returns: { data, model }

### Embeddings — $0.01
POST /api/embed
Body: { "input": "text" }  or  { "input": ["a","b"] }  (max 64)
Returns: { embeddings, dimensions, count }

### Search — $0.01
GET /api/search?q=your+query&count=10
Returns: { query, results: [{title, url, description}], provider }

### Wallet & token risk — $0.01
GET /api/risk/pro?address=0x...&chain=base&type=wallet
Checks OFAC sanctions, 3,000+ scam/phishing addresses, honeypot and rug signals.
Returns: { risk_score, risk_level, verdict, flags, reasons, signals, sources }

### Risk (free demo) — $0.00
GET /api/risk?address=0x...&chain=base

### Universal lookup (free) — $0.00
GET /api/lookup?address=<any address, any chain>
No chain parameter — EVM and XRPL are auto-detected from the address format.
Optional ?chain=ethereum hint (EVM only; default base).
Returns: { address, chain, chain_detected, exists, risk_score, risk_level, verdict, flags, entity, upgrade }
Same field names for every chain.

### Entity attribution (free) — $0.00
GET /api/entity?address=0x...&chain=base|ethereum   (default base)
What IS this address — not who owns it.
Returns: { address, chain, is_known, label, category }
category: sanctioned | mixer | exchange | bridge | dex_router | scam | drainer | phishing | token_contract | protocol | unknown
Sources: OFAC, ScamSniffer, MEW darklist, eth-labels, curated Base/Tornado sets. Never guesses.

### Name resolution (free) — $0.00
GET /api/resolve?q=<name or address>
Bidirectional. ENS (name.eth) and Basenames (name.base.eth), forward and reverse,
via public RPC + the resolver contracts directly. Reverse records are forward-verified
against the input address (anti-spoofing). Auto-detects direction from the query.
Returns: { query, kind, namespace, chain, resolved, address, name, sources, lookup }
XRPL .xrp names are detected but return resolved:false — no canonical registry.

### Wallet caller ID (free) — $0.00
GET /api/callerid?address=0x...&chain=base|ethereum   (default base)
Should this wallet be answered? Wallet messaging (XMTP, Push) has no spam filter.
Composes entity attribution + risk score + on-chain history + verified name into one answer.
Returns: { address, chain, name, entity, exists, risk_score, risk_level, verdict, recommendation, confidence, reasons, checked_at }
recommendation: ANSWER | SCREEN | BLOCK
confidence: high (entity + risk both checked) | low (an input degraded — treat as "unchecked", not "clean")
reasons[] names exactly what drove the call. SCREEN is the default for unknown addresses, not a failure.
BLOCK: sanctioned | drainer | phishing | scam | mixer, or risk verdict BLOCK.
ANSWER: known exchange/dex_router/protocol, or a forward-verified ENS/Basename — each with risk verdict PROCEED.

### Wallet blocklist — Shield (free) — $0.00
Per-user PRIVATE blocklist. Your block never affects anyone else's results — no shared
reputation, no aggregate scoring. First stateful product in the store.

Ownership is proved by signature, not an API key:
1. GET /api/shield/nonce?owner=0x...            -> { nonce, expires_at }  (10 min, single-use)
2. Sign the returned message with the owner key (EIP-191 / personal_sign).
   v1 verifies EOA (ECDSA) signatures only — ERC-1271 / Safe wallets are not supported yet.
3. POST /api/shield/session { owner, signature } -> { token }   (bearer, 60 min, for reads)

Writes (fresh nonce + signature each, the message binds the action and target):
POST /api/shield/block   { owner, signature, address, chain?, reason? }
POST /api/shield/unblock { owner, signature, address }
POST /api/shield/allow   { owner, signature, address, reason? }   ("I vouch for this address")
POST /api/shield/unallow { owner, signature, address }

Reads (Authorization: Bearer <token>):
GET /api/shield/list                              -> { blocks[], allows[] }
GET /api/shield/check?address=0x...&chain=base    -> { blocked, source, recommendation, reasons[], entity, name }
GET /api/shield/stats                             -> 7d / 30d / 90d counts by action and entity category

/check precedence: sanctioned entity (hard stop, cannot be allowlisted) > your allowlist >
your manual block > auto-BLOCK from caller ID > the caller-ID recommendation (ANSWER|SCREEN|BLOCK).
Every /check is logged so /stats is real. shield_events are pruned at 90 days — 90-day retention, stated not buried.

## Payment
x402 protocol. Network: Base (eip155:8453). Asset: USDC.
Discovery: https://uxus.finance/.well-known/x402.json
MCP endpoint: https://uxus.finance/api/mcp
